# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CareTaker' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CareTaker

  pod 'Firebase/Core'
  pod 'FirebaseDatabase'
  pod 'FirebaseAuth'
  pod 'FirebaseStorage'

  target 'CareTakerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CareTakerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
